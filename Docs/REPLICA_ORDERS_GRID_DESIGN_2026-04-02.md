<!-- DOC_ENCODING_REQUIREMENT_UTF8 -->
# Дизайн таблицы на OLV: требования и acceptance (2026-04-02)

## 1. Цели дизайна
1. Визуально сохранить привычный стиль для операторов.
2. Поведенчески сохранить текущие сценарии без переобучения.
3. Улучшить плавность и отзывчивость при больших списках.

## 2. Что переносим из текущего вида
1. Палитра строк (base/zebra/selected/hover).
2. Разделение групповых и одиночных заказов.
3. Статусная индикация и читаемые подписи.
4. Отдельная панель фильтров (`flpFilters`) вне заголовка таблицы.

## 3. OLV дизайн-реализация
1. Иерархия: `TreeListView`.
2. Визуал: `FormatRow/FormatCell` + row/header palette, `RowHeight`, `CellPadding`.
3. Сортировка: включена, с индикаторами.
4. Статусные иконки в первой колонке (`SmallImageList` + `ImageGetter`).
5. Stage UX: `hand` cursor и tooltip в file-stage ячейках.
6. Контекстные меню строки и stage-ячеек с иконками и быстрыми действиями.

## 4. UX требования
1. Первая колонка показывает `№ заказа` (без превращения одиночных заказов в файловые имена).
2. Expand/collapse работает мышью и клавиатурой.
3. Быстрый фильтр не ломает дерево и selection.
4. Tooltip ошибок статуса отображается корректно.
5. Контекстное меню и drag-drop остаются предсказуемыми.

## 5. Acceptance checklist
1. Группы и одиночные заказы визуально различимы.
2. Сортировка по колонкам стабильна и с индикатором.
3. Скролл на длинных списках без заметных фризов.
4. Нет мерцания при hover/selection.
5. Синхронизация с tiles сохраняет текущую выбранную сущность.

## 6. Практическое замечание
Текущий OLV prototype в `OrdersTreePrototypeForm` уже используется как визуальная и поведенческая заготовка для production migration. Следующий шаг — перенос его ядра в `OlvOrdersAdapter`.
