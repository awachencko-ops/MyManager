# Комплексное исследование MyManager и план работ

Дата: 2026-03-13  
Основание: анализ текущего состояния кода и фактические изменения структуры `MainForm`.

## 1. Цель документа

Сформировать единый рабочий документ, который:
1. Фиксирует текущее состояние программы в целом (архитектура, данные, UX, обработка заказов).
2. Показывает реальные технические риски и узкие места.
3. Дает пошаговый рабочий план: закрыть single-first миграцию и подготовить контролируемый переход к multi-заказам.

## 2. Снимок системы (AS-IS)

### 2.1 Платформа и состав

- Стек: `WinForms`, `.NET 8`, C# (`MyManager.csproj`).
- Точка входа: `Program.cs` -> `Application.Run(new MainForm());`.
- Legacy-формы из `Forms/Archive` исключены из сборки (`Compile Remove="Forms\\Archive\\**\\*.cs"`).
- Конфиги/история: JSON (`settings.json`, `history.json`, `pitstop_actions.json`, `imposing_configs.json`), рабочие пути переведены на NAS.
- Добавлен сетевой контур hotfolder: `\\NAS\...\MYMANAGER BASEFOLDER\WARNING NOT DELETE\{HotImposing|PitStop}`.

### 2.2 Карта модулей (обновлено)

| Блок | Назначение | Текущее состояние |
|---|---|---|
| `Forms/MainForm/MainForm.cs` | Core orchestration формы (инициализация, wiring) | Done, сильно облегчён |
| `Forms/MainForm/Core/*` | State, lifecycle заказов, status strip, status cell visuals | Done |
| `Forms/MainForm/FileOps/*` | Контекстное меню, grid interactions, stage/file ops, clipboard/dragdrop | Done |
| `Forms/MainForm/Filters/*` | Queue, popup-фильтры, применение фильтров/подсчётов | Done |
| `Forms/MainForm/Views/*` | Плиточный view/thumbnail логика | Done |
| `Services/OrderProcessor.cs` | Run pipeline (PitStop/Imposing), timeout, progress, multi-item параллелизм | Рабочий |
| `Services/OrderTopologyService.cs` | Нормализация group-first модели, marker `SingleOrder/MultiOrder`, legacy-sync | Рабочий |
| `Services/ConfigService.cs` | Загрузка/сохранение `pitstop/imposing` конфигов | Рабочий, переведен на пути из `AppSettings` |
| `Models/OrderData.cs` + `OrderFileItem.cs` | Контракт заказа и item-уровня | Рабочий, есть legacy-поля order-level путей |
| `Forms/Managers/*`, `Forms/Selectors/*`, `Forms/Orders/*`, `Forms/Settings/*` | Диалоги и менеджеры, разнесены по смысловым папкам | Done |

### 2.3 Кодовая нагрузка (факты)

- `MainForm`-core файл: `513` строк (ранее был монолит >4k строк).
- `MainForm` разложен на `17` partial-файлов по зонам ответственности.
- Сборка baseline на 2026-03-13: `0 warnings`, `0 errors`.

Вывод: функциональность сохранена, техдолг в виде «единого монолита MainForm» существенно снижен.

## 3. Состояние миграции (факт по текущему этапу)

### 3.1 Что уже закрыто

1. Старт приложения только через `MainForm`.
2. Legacy `Forms/Archive` исключен из сборки.
3. Контекстное меню и операции по стадиям в новой форме.
4. Run/Stop/Status/Progress интеграция с `OrderProcessor`.
5. StatusStrip-индикаторы (текст, progress, stats, connection, disk, alerts).
6. Nullable-cleanup (сборка без предупреждений).
7. Group-first нормализация (`SingleOrder`/`MultiOrder`) внедрена в pipeline/историю.
8. Крупная декомпозиция `MainForm` выполнена: partial-файлы + структурирование по папкам.
9. Выполнен NAS-cutover для рабочих путей:
   - `OrdersRootPath` -> `\\NAS\...\MYMANAGER BASEFOLDER\Orders`
   - `TempFolderPath` -> `\\NAS\...\MYMANAGER BASEFOLDER\Orders\TempMyManager` (+ `in/prepress/print`).
10. Выполнен перенос `C:\HotImposing` и `C:\PitStop` в `\\NAS\...\WARNING NOT DELETE\HotImposing|PitStop`.
11. `imposing_configs.json` и `pitstop_actions.json` централизованы в `\\NAS\...\Config\...`, пути внутри JSON переписаны на NAS.
12. `qihot4.xml` экспортирован с NAS-путями (`%APPDATA%\\Quite\\Preferences\\qihot4.xml`), сделан backup.
13. Исправлен источник ошибки сборки `Duplicate ... Attribute`: исключены `artifacts\**\*.cs` из компиляции.
14. Реализована визуализация статусов в колонке `Состояние` (иконка + фон + текст) для всех рабочих статусов.
15. Высота рабочих строк `dgvJobs` настроена под квадратный блок статуса (`RowTemplate.Height = 42`), заголовок оставлен отдельно (`ColumnHeadersHeight = 34`).

### 3.2 Что реально осталось по обязательному контуру

1. Пользовательский фильтр: сейчас используется заглушка (`FilterUsers`) и не подключен к реальному источнику данных.
2. Финальная parity-регрессия `MainForm` против контрольного набора сценариев (single-focus).
3. Дочистка string-контрактов (status/stage/column-name) до централизованного typed-контура.
4. Вывести новые NAS-пути `PitStop/Imposing` в UI настроек (сейчас уже работают через `settings.json`, но без отдельных полей в форме).

### 3.3 Обновление за 2026-03-12 (факт)

1. Подтверждён путь Quite Hot Imposing: `%APPDATA%\Quite\Preferences\qihot4.xml`; файл синхронизирован с NAS-путями секвенций.
2. Рабочие пути заказов и temp окончательно переключены на NAS:
   - `OrdersRootPath` -> `\\NAS\...\MYMANAGER BASEFOLDER\Orders`
   - `TempFolderPath` -> `\\NAS\...\MYMANAGER BASEFOLDER\Orders\TempMyManager`
3. Для `TempMyManager` на NAS создана и проверена структура `in/prepress/print`.
4. В UI-настройках улучшена формулировка поля параллелизма:
   - было: `Параллельных item (0=без лимита)`
   - стало: `Параллельных файлов (мульти-заказ)`, подсказка: `0 — без ограничений (все файлы сразу)`.

### 3.4 Обновление за 2026-03-13 (факт)

1. В `MainForm` добавлен полный маппинг визуальных состояний в таблице заказов:
   - `Обработано`, `В архиве`, `Выполняется сборка`, `Обрабатывается`, `Ожидание`, `Отменено`, `Ошибка`, `Завершено` (+ alias `Напечатано`).
2. Введён точный выбор иконки по схеме `папка + fileNameHint`, чтобы исключить путаницу при совпадающих каталогах/наименованиях.
3. Для статуса `Отменено` основной источник иконки переключен на `Icons\cancel` (с fallback на `file export/stop` для устойчивости).
4. В проекте включено копирование всех PNG-иконок в output: `Icons\**\*.png` (`CopyToOutputDirectory=PreserveNewest`).
5. Сборка после изменений: успешно, `0 warnings`, `0 errors`.

## 4. Ключевые наблюдения и риски

| ID | Приоритет | Наблюдение | Риск |
|---|---|---|---|
| R1 | Средний | `MainForm` всё ещё крупный как aggregate (хотя монолит уже разрезан) | Сложность будущих изменений, если не продолжить детализацию |
| R2 | Высокий | Фильтр пользователей не связан с фактическими данными заказов | Некорректная фильтрация и ложные ожидания пользователя |
| R3 | Средний | Multi-order backend уже есть, UI временно single-only | Разрыв между возможностями модели и интерфейсом |
| R4 | Средний | Есть string-контракты (имена колонок, статусы, маркеры) | Хрупкость при рефакторинге |
| R5 | Средний | Остатки legacy-подхода: order-level пути плюс item-level пути | Риск расхождений данных |
| R6 | Средний | Нет автоматических тестов (unit/integration/smoke) | Регрессии выявляются только вручную |
| R7 | Низкий | Остаточные проблемы кодировки отдельных UI-строк | UX-шум и поддерживаемость |
| R8 | Средний | LAN-контур формально не закреплён как отдельный feature-gate | Непредсказуемость при доступе к сетевым папкам |

## 5. Целевое решение (TO-BE, прагматичный вариант)

### 5.1 Архитектурная цель

Оставить WinForms и текущую функциональность, но довести модульность до состояния, когда изменения не требуют правок в большом числе разнородных файлов.

### 5.2 Целевые принципы

1. `MainForm` = orchestration + wiring, без дублирования бизнес-логики.
2. Доменная истина по топологии и путям: `OrderTopologyService` + item-уровень.
3. Строгая терминология в коде/доках: `single-order` / `multi-order`.
4. Сначала стабильный single-first контур, затем отдельным этапом полноценный multi-order UI.
5. LAN-интеграция вводится поэтапно: от минимального контроля доступности к полноценному источнику данных.

### 5.3 Технический целевой контур

1. Довести декомпозицию `MainForm` до мелких стабильных модулей.
2. Централизовать stage/file операции в выделенный helper/service (без дублирования между UI-сценариями).
3. Ввести typed-константы/enum для статусов, stage, column IDs.
4. Подготовить UI-слой для multi-order без включения функционала до завершения single-gates.
5. Зафиксировать минимальный LAN baseline как отдельный deliverable.

## 6. Поэтапный план работ

## Этап A: Закрытие текущей миграции (single-first final)
Срок: 1-2 рабочих дня

1. Подключить реальный источник пользователей и починить user-filter end-to-end.
2. Провести полный ручной регрессионный прогон по согласованному single-checklist.
3. Зафиксировать и закрыть оставшиеся legacy-алиасы именования в активном коде/доках.
4. Обновить этот документ по фактическому статусу Go/No-Go.

Definition of Done:
1. User-filter реально фильтрует данные, а не заглушки.
2. Нет блокирующих дефектов по чеклисту single-сценариев.
3. Документально закрыт этап "migration on MainForm (single-first)".

## Этап B: Техническая стабилизация и декомпозиция
Срок: 3-5 рабочих дней

1. Разрезать `MainForm` на логические partial-файлы без изменения поведения. (выполнено в крупном объеме)
2. Централизовать stage/file операции (добавление, удаление, rename, copy/move). (in progress)
3. Убрать string-магии в ключевых контрактах (status/stage/columns). (pending)
4. Нормализовать проблемные строки кодировки в UI/сообщениях. (in progress)

Definition of Done:
1. Поведение идентично baseline, но структура кода модульная.
2. Снижение когнитивной нагрузки при изменениях в фильтрах, статусах, файловых операциях.

## Этап B1: LAN интеграция (практичный план)
Срок: 1-2 рабочих дня

1. Вынести источник пользователей в сетевой файл (`users.json` или `users.csv` на UNC-пути).  
   Статус: инфраструктура NAS уже готова, файл `users.json` создан; загрузка в `FilterUsers` еще не подключена.
2. Реализовать загрузку пользователей при старте с периодическим обновлением (без рестарта формы).
3. Добавить локальный кэш последней успешной версии списка пользователей.
4. Зафиксировать fallback-поведение: при недоступности сети использовать кэш и показать статус автономного режима.

Definition of Done:
1. User-filter заполняется из сетевого источника.
2. При недоступной сети приложение продолжает работу через кэш без падений.
3. Индикатор `toolConnection` корректно отражает онлайн/офлайн.

## Этап C: Реализация полноценного multi-order UI
Срок: 5-8 рабочих дней

1. Добавить визуальную модель group/item в таблице (expand/collapse, item rows).
2. Добавить item-level действия в UI (без возврата legacy-веток).
3. Реализовать запуск selected items + отображение item progress/status.
4. Добавить сценарии создания/редактирования multi-заказа в формах.

Definition of Done:
1. Multi-order проходит полный цикл от создания до печатного вывода в UI.
2. Single-order остается частным случаем group-first без отдельной архитектуры.

## Этап D: Контроль качества и релизная фиксация
Срок: 2-3 рабочих дня

1. Ввести минимальный smoke-набор (скрипт/чеклист + стабильный dataset).
2. Проверить race/parallel сценарии (`MaxParallelism`, stop/cancel, timeout).
3. Зафиксировать release notes, ограничения и known issues.

Definition of Done:
1. Повторяемый прогон smoke без блокирующих дефектов.
2. Документированная готовность к производственной эксплуатации.

## 7. Приоритет ближайших шагов (что делать следующим)

1. Закрыть Этап A, пункт 1: реальный источник пользователей и рабочий user-filter.
2. Запустить Этап B1 (практичная LAN-интеграция пользователей).
3. Провести полный single-regression (Этап A, пункт 2).
4. Дожать Этап B (typed-контракты + финальная чистка кодировки).

### 7.1 Зафиксированные решения по внешним референсам (2026-03-13)

1. Для автотестов в текущем этапе берем только:
   - `FlaUI` для UI smoke (3-5 сценариев),
   - `Verify` для snapshot-проверок JSON/моделей/конфигов.

## 8. Критерии управления риском

1. Любое изменение `MainForm` проходит через ручной smoke по ключевым single-сценариям.
2. Любое изменение модели заказа сначала прогоняется через `OrderTopologyService.Normalize(...)`.
3. Для этапа multi-order не смешивать разработку с "поддержкой legacy UI"; только новый `MainForm`-контур.
4. Для LAN-фич фиксировать fallback-поведение в offline-режиме до релиза.

## 9. Подключение к локальной сети: варианты

### Вариант 1 (базовый, "для галочки")

1. Использовать UNC-пути в настройках (`\\\\server\\\\share\\\\...`) для рабочих папок.
2. Проверять доступность по существующему индикатору `toolConnection`.
3. В случае недоступности пути работать в автономном режиме с явным статусом `Сервер: автономно`.

Плюсы: быстро, почти без новых рисков, уже частично есть в коде.  
Минусы: это контроль доступности, а не полноценная сетевой синхронизация пользователей/данных.

### Вариант 2 (практичный) — рекомендован сейчас

1. Хранить `users.json`/`users.csv` на сетевой шаре.
2. Загружать список пользователей при старте и периодически обновлять.
3. Держать локальный кэш на случай недоступности сети.

Плюсы: решает текущую задачу по user-filter.  
Минусы: нужен контроль конфликтов/версий и fallback-логика.

### Вариант 3 (зрелый)

1. Центральный сервис/API или БД.
2. Авторизация, аудит, централизованная очередь/статусы.

Плюсы: масштабируемо и управляемо.  
Минусы: высокий порог внедрения и сроки.

Итог по текущему этапу: берем **Вариант 2 (практичный)** как основной план внедрения.

---

Статус документа: актуален на 2026-03-13, синхронизирован с текущим состоянием рабочего дерева проекта (включая NAS-cutover, Quite Hot Imposing export и визуальную систему статусов в `MainForm`).
