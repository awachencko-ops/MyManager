# Аудит статусов и триггеров заказа

## 1) Полный список статусов, которые реально используются в коде

### 1.1 Базовые/ручные статусы
- `⚪ Ожидание`
- `📂 В работе`
- `✅ Готово`
- `📦 В архиве`

### 1.2 Статусы из автоматического процессора (`OrderProcessor`)
- `🟡 Запуск…`
- `Новый`
- `🟡 PitStop: копирование`
- `🟡 PitStop OK`
- `🟡 PitStop готово`
- `🟡 Imposing: старт`
- `✅ Готово`
- `🔴 Ошибка`

## 2) Триггеры (когда и почему статус меняется)

## 2.1 Создание/добавление заказа
1. **Новый заказ из формы** → `📂 В работе`.
2. **Быстрое добавление заказа (в главной форме)** → `⚪ Ожидание`.

### Риск
Один и тот же «новый» бизнес-сценарий начинается с разных статусов в зависимости от способа создания записи.

## 2.2 Изменение путей файлов вручную (`UpdateOrderFilePath`)
1. Если `PrintPath` указывает на существующий файл → `✅ Готово`.
2. Иначе если `PreparedPath` указывает на существующий файл и текущий статус НЕ содержит `Ошибка` → `📂 В работе`.
3. Иначе если `SourcePath` пустой → `⚪ Ожидание`.

### Риски
- Статус может не обновиться в ожидаемое состояние, если `SourcePath` не пустой, а `PreparedPath` и `PrintPath` пустые/битые.
- Проверка `Contains("Ошибка")` не защищает от будущих вариантов текста ошибок (например, если формат изменится).

## 2.3 Автопроцесс (`OrderProcessor.ProcessOrder`)
Последовательность уведомлений:
1. Старт обработки → `🟡 Запуск…`
2. Если сценарии не выбраны (`pitCfg == null && impCfg == null`) → `Новый` и выход из метода.
3. PitStop этап:
   - копирование во вход hotfolder → `🟡 PitStop: копирование`
   - после успеха ожидания файлов → `🟡 PitStop OK`
   - после сохранения версии в stage 2 (в extended) → `🟡 PitStop готово`
4. Imposing этап:
   - старт копирования → `🟡 Imposing: старт`
5. При успешном завершении → `✅ Готово`
6. При исключении → `🔴 Ошибка`

### Ключевой риск
Статус `Новый` выпадает из общей таксономии (`Ожидание/В работе/Готово/Ошибка/Архив`) и визуально/логически может трактоваться неоднозначно.

## 2.4 Архиватор статуса (`RefreshArchivedStatuses`)
1. Если заказ распознан как архивный (`IsOrderInArchive`) → принудительно `📦 В архиве`.
2. Если заказ больше не архивный, но статус был `📦 В архиве`:
   - если есть существующий `PrintPath` → `✅ Готово`
   - иначе → `⚪ Ожидание`

### Риски
- Выход из архива может потерять промежуточный смысл (`📂 В работе`) даже если есть подготовленный файл.
- Приоритет «архивности» выше всех остальных статусов (включая ошибки), что может скрыть важные инциденты.

## 3) Где возникает «неразбериха» (диагноз)

1. **Смешение бизнес-статусов и тех. прогресса.**
   Например, `🟡 PitStop OK`/`🟡 Imposing: старт` — это скорее runtime-этапы, а `✅ Готово`/`🔴 Ошибка` — итоговые состояния.

2. **Нет единого словаря статусов.**
   Строковые литералы разбросаны по нескольким файлам, включая разные формулировки (`Новый`, `⚪ Ожидание`, `📂 В работе`).

3. **Конкурирующие «авто-источники истины».**
   - `OrderProcessor` пушит статусы через событие.
   - `UpdateOrderFilePath` пересчитывает статус по файлам.
   - `RefreshArchivedStatuses` перезаписывает статус по признаку архива.

4. **Статус зависит от наличия файлов (File.Exists), а не только от состояния процесса.**
   Любые сетевые/дисковые лаги дают ложные переходы.

## 4) Предложения по исправлению

## 4.1 Ввести нормализованную модель статуса (минимум)
- Бизнес-статус (enum):
  - `Waiting`
  - `InProgress`
  - `Done`
  - `Error`
  - `Archived`
- Технический подстатус/этап (string или enum):
  - `Starting`, `PitStopCopy`, `PitStopDone`, `ImposingStart`, и т.д.

UI показывает: `Бизнес-статус + (этап)`.

## 4.2 Централизовать переходы в одном месте
Сделать единый `StatusService` / state machine:
- Все смены статуса идут через один метод.
- Явные правила приоритета:
  - `Archived` выше `Done/InProgress/Waiting`, но ниже активного `Error` (или по вашему правилу).
  - `Error` не затирается авто-логикой путей до ручного/явного резолва.

## 4.3 Убрать «магические строки»
- Словарь/enum + маппер в UI.
- Отдельный словарь человекочитаемых текстов/эмодзи (локализация).

## 4.4 Разделить причины обновления
Хранить метаданные последнего перехода:
- `ChangedBy = Manual | Processor | ArchiveSync | FileSync`
- `ChangedAt`
- `Reason`

Это сильно упростит разбор «почему статус внезапно поменялся».

## 4.5 Добавить аудит-лог переходов
Минимальный журнал: `orderId, oldStatus, newStatus, trigger, timestamp`.

## 5) Быстрый план внедрения (без крупного рефакторинга)
1. Согласовать канонический список бизнес-статусов (5 штук).
2. Заменить `"Новый"` на `"⚪ Ожидание"` или `"📂 В работе"` по бизнес-правилу.
3. В `UpdateOrderFilePath` и `RefreshArchivedStatuses` запретить неявно затирать `🔴 Ошибка`.
4. Добавить поле `ProcessingStage` (или `LastProgressMessage`) для временных `🟡`-этапов.
5. Вывести в UI одновременно `Status` + `ProcessingStage`.

## 6) Вопросы для уточнения (чтобы закрыть первопричины)
1. Какой статус должен быть у **нового** заказа по умолчанию: `⚪ Ожидание` или `📂 В работе`?
2. Что по бизнес-логике означает статус `Новый` и нужен ли он вообще?
3. Должен ли `📦 В архиве` скрывать `🔴 Ошибка`, если ошибка не разобрана?
4. Когда ошибка считается «снятой»: вручную оператором или автоматически при следующем успешном шаге?
5. Можно ли считать наличие `PrintPath` + `File.Exists` достаточным критерием `✅ Готово`, или нужен отдельный флаг завершения процесса?
6. Нужна ли отдельная колонка «Этап обработки» вместо хранения промежуточных `🟡` в основном статусе?
7. Есть ли SLA/таймауты, при которых заказ должен переходить в специальный статус (например, `⏸ Ожидание ресурса`)?
8. Нужен ли отчет «кто/что изменило статус» для разборов ошибок (оператор, автомат, архиватор)?
