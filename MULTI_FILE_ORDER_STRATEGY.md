# Переход от 1 файла к 2+ файлам на заказ (обновлённая стратегия)

## 1) Подтверждённые решения (зафиксировано по вашим ответам)

## 1.1 Бизнес-модель
- В одном заказе могут быть:
  - как файлы одного изделия,
  - так и разные варианты/виды изделия (в т.ч. разные размеры), но в рамках одной смысловой задачи.
- Сквозная связка **`исходный -> подготовка -> печать`** нужна **для каждого файла**.
- Разрешён частичный статус заказа: например, `⚠ Частично готово (3/5)`.
- Нужны сортировка/группировка файлов в заказе (минимум по имени и дате).
- Нужны массовые операции (применить обработку ко всем файлам).
- Нужен запрет дубликатов имён внутри одной стадии.
- Нужно хранить «основной файл для клиента» отдельно от «технических файлов».

## 1.2 Режим обработки
- Запуск обработки — **с выбором перед запуском** (что именно обрабатывать).
- Режим по умолчанию — **обрабатывать все файлы**.
- Активный файл как основная модель — **не обязателен** (fallback допускается только для одиночного заказа).
- Нужна параллельная обработка.
- Ограничения на число файлов не нужны (обычно до 5, но возможны 50+).

## 1.3 UX-модель
- Заказ в списке по умолчанию **свёрнут**.
- При разворачивании показывается список файлов внутри заказа.
- Должна быть «рабочая строка» (пустая строка добавления):
  - в неё кладут файл,
  - автоматически создаётся новая пустая строка.
- Дополнительный сценарий:
  - если в заказе 1 файл, он ведёт себя как обычный,
  - по ПКМ: «Превратить в группу» -> заказ становится группой (свёрнутая структура, внутри файл + рабочая строка).

---

## 2) Целевая архитектура (вариант C, полный)

С учётом приоритета «**полная разработка**», целевая модель — полнофункциональная многофайловая обработка.

## 2.1 Единица обработки
- Внешне — **заказ**.
- Внутри заказа — список файлов (items), каждый проходит собственный pipeline.
- Статус заказа = агрегат по статусам файлов.

## 2.2 Модель данных (векторный массив)
Да, «векторный массив» реализуем через динамические коллекции (`List<T>` в C#). Это фактически вектор.

### Предложенная модель

```csharp
public class OrderData
{
    public string InternalId { get; set; }
    public string Id { get; set; }

    // Новый контейнер (вектор) файлов заказа
    public List<OrderFileItem> Items { get; set; } = new();

    // Агрегированный статус заказа
    public string Status { get; set; } = "⚪ Ожидание";

    // Метаданные и существующие поля заказа...
}

public class OrderFileItem
{
    public string ItemId { get; set; } = Guid.NewGuid().ToString("N");

    // Бизнес-представление
    public string ClientFileLabel { get; set; } = "";   // Основной файл для клиента
    public string Variant { get; set; } = "";           // Вид/размер/версия

    // Сквозные стадии
    public string SourcePath { get; set; } = "";
    public string PreparedPath { get; set; } = "";
    public string PrintPath { get; set; } = "";

    // Технические вложения
    public List<string> TechnicalFiles { get; set; } = new();

    // Состояния
    public string FileStatus { get; set; } = "⚪ Ожидание";
    public string LastReason { get; set; } = "";
    public DateTime UpdatedAt { get; set; } = DateTime.Now;

    // Порядок поступления (FIFO для сопоставления вход->выход)
    public long SequenceNo { get; set; }
}
```

### Почему это подходит
- `List<OrderFileItem>` = ваш «векторный массив».
- Есть пер-файловая сквозная цепочка.
- Есть место для разделения клиентских и технических файлов.
- Можно корректно считать частичную готовность.

---

## 3) Правила обработки и статусов

## 3.1 Правила запуска
Перед запуском показывать диалог выбора:
1. `Обработать все файлы` (по умолчанию).
2. `Обработать только выбранные`.
3. `Применить одинаковые операции ко всем` / `разные операции по файлам`.

## 3.2 FIFO-сопоставление
Вы подтвердили: `первый пришёл -> первый вышел`.
- Для этого хранить `SequenceNo` при добавлении файла.
- При batch-обработке поддерживать стабильный порядок.

## 3.3 Статусы

### Статус файла (item)
- `⚪ Ожидание`
- `🟡 В работе`
- `✅ Готово`
- `🔴 Ошибка`

### Агрегированный статус заказа
- Все `Ожидание` -> `⚪ Ожидание`
- Все `Готово` -> `✅ Готово`
- Все `Ошибка` -> `🔴 Ошибка`
- Смешанный прогресс -> `⚠ Частично готово (X/Y)`
- Есть хоть один `В работе` -> `🟡 В работе (X/Y)`

---

## 4) UX-решение: заказ-группа

## 4.1 Представление
- Таблица заказов остаётся верхним уровнем.
- Строка заказа раскрывается во вложенный список `Items`.
- Внизу вложенного списка всегда «рабочая строка» для drag&drop/выбора файла.

## 4.2 Поведение «рабочей строки»
- Пустая строка принимает файл.
- После заполнения автоматически создаётся новая пустая строка.
- Если строка очищена — удаляется, но одна пустая строка всегда остаётся.

## 4.3 Миграция в группу
- Если заказ однофайловый, ПКМ по заказу:
  - `Превратить в группу`.
- После команды создаётся `Items[0]` из текущих путей + новая рабочая строка.

---

## 5) Массовые операции

Поддержать (минимум):
1. Применить PitStop ко всем выбранным `Items`.
2. Применить Imposing ко всем выбранным `Items`.
3. Массовое копирование/перемещение/удаление по стадии.
4. Массовая проверка валидности путей.

Важно: массовые операции должны работать и на выделении, и на «всем заказе».

---

## 6) Параллельная обработка

Вы хотите «без ограничений», но технически для устойчивости продукта лучше:
- сделать параметр в настройках `MaxParallelism`;
- значение `0` трактовать как «без ограничений (как requested)»;
- рекомендованный safe default (например 4–8) для слабых ПК.

Это не ограничивает вас — просто добавляет защиту от зависаний/перегрузки.

---

## 7) Миграция старой истории (`history.json`)

Вы обозначили: «переписать старую под новую». Предлагаемый безопасный путь:

1. При первом запуске новой версии:
   - создать `history.json.bak`.
2. Для каждого старого заказа:
   - создать `OrderFileItem` из старых `SourcePath/PreparedPath/PrintPath`.
3. Заполнить `Items` и пересчитать агрегированный статус.
4. Сохранить в новый формат.

Если миграция не удалась — откат к `.bak` + сообщение пользователю.

---

## 8) Этапы реализации (полный путь)

## Этап 1 — Data/Storage
- Ввести `OrderFileItem` и `Items`.
- Реализовать миграцию старого JSON в новый формат.
- Обновить сохранение/загрузку, логирование по item.

## Этап 2 — UI групп и рабочей строки
- Добавить иерархическое отображение заказа и файлов.
- Реализовать сворачивание/разворачивание.
- Реализовать «рабочую строку» автодобавления.
- Добавить действие «Превратить в группу».

## Этап 3 — Pipeline и batch
- Перевести процессор на обработку списка `Items`.
- Реализовать диалог выбора режима запуска (все/выбранные).
- Добавить массовые операции.
- Ввести агрегированный статус заказа (`X/Y`).

## Этап 4 — Производительность и стабилизация
- Параллельная обработка с конфигом.
- Оптимизация больших заказов (50+).
- Тесты на дубликаты имён, FIFO и частичные успехи.

---

## 9) Технические решения, которые осталось утвердить

1. Где хранить сортировку внутри заказа: глобально или per-order?
2. Формат `Variant` (свободный текст или справочник)?
3. Разрешать ли ручную перестановку `SequenceNo` (нарушая FIFO)?
4. Что делать при одном успешном и одном ошибочном файле в batch:
   - оставлять `⚠ Частично готово`,
   - или поднимать заказ в `🔴 Ошибка`?
5. Как отображать дубликаты до сохранения: автопереименование (`_1`, `_2`) или блокирующая ошибка?

---

## 10) Рекомендация
С учётом ваших ответов правильный путь — **полная модель C**:
- заказ как контейнер,
- файлы как вектор `List<OrderFileItem>`,
- групповой UI с рабочей строкой,
- batch-обработка с выбором перед запуском,
- агрегированные статусы `X/Y`.

Это даёт ровно тот UX/flow, который вы описали, и масштабируется от 1 до 50+ файлов в заказе.
